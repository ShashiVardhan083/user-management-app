<nav class="navbar navbar-expand-lg navbar-dark bg-dark clean-nav fixed-top">
    <div class="container">
        <a class="navbar-brand fw-semibold"
           asp-controller="Home"
           asp-action="Index">
            UserManagement
        </a>

        <ul class="navbar-nav ms-auto align-items-center">

            <li class="nav-item">
                <a class="nav-link"
                   asp-controller="Home"
                   asp-action="Index">
                    Home
                </a>
            </li>

            @if (User.Identity.IsAuthenticated)
            {
                <li class="nav-item ms-3 text-light">
                    @User.Identity.Name
                </li>

                <li class="nav-item ms-3">
                    <a asp-controller="Account"
                       asp-action="Logout"
                       class="btn btn-outline-light btn-sm">
                        Logout
                    </a>
                </li>
            }
        </ul>
    </div>
</nav>
